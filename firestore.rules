rules_version = '2';

// PHASE 1 PLACEHOLDER: lock everything down. Real rules in Phase 2.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
